---
import astroLogo from '../assets/astro.svg';
---

<div class="container">
  <div class="gradient-background">
    <div class="apple-gradient"></div>
  </div>
  
  <main>
    <h1>언어를 사랑하는 사람과<br /> 함께 가장 빠른 학습 여정을 시작하세요</h1>
  </main>
</div>

<style>
  @import url(//spoqa.github.io/spoqa-han-sans/css/SpoqaHanSansNeo.css);

  /* Reset and base styles */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --primary: #4d21fc;
    --secondary: #7828c8;
    --accent: #f41cf4;
    --background: #F8FAFC;
    --text: #000000;
    --text-light: #ffffff;
    --text-muted: rgba(0, 0, 0, 0.7);
    --radius: 12px;
    --shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
  }

  body {
    background-color: var(--background);
    color: var(--text);
    font-family: 'Spoqa Han Sans Neo', 'Inter', system-ui, sans-serif;
  }

  /* Updated gradient background with expansion animation */
  .gradient-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background-color: #F8FAFC; /* gentle base */
    overflow: hidden;
  }

  .apple-gradient {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 30% 30%, #A7F3D0 0%, transparent 50%),
                radial-gradient(circle at 70% 50%, #FFD1A6 0%, transparent 60%),
                radial-gradient(circle at 50% 80%, #A6D5FF 0%, transparent 70%);
    opacity: 0;
    transform-origin: bottom center;
    transform: scale(0.01);
    filter: blur(40px) saturate(1.2);
    animation: gradient-expand 4.5s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    will-change: transform, opacity, filter;
  }

  @keyframes gradient-expand {
    0% {
      opacity: 0;
      transform: scale(0.01);
      filter: blur(40px) saturate(1.2);
    }
    30% {
      opacity: 0.43;
      transform: scale(0.5);
      filter: blur(30px) saturate(1.12);
    }
    70% {
      opacity: 0.78;
      transform: scale(0.93);
      filter: blur(20px) saturate(1.04);
    }
    100% {
      opacity: 1;
      transform: scale(1.2);
      filter: blur(15px) saturate(1);
    }
  }

  /* Add soft noise texture */
  .apple-gradient::after {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='2' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 700;
    text-align: center;
    line-height: 1.4;
    max-width: 800px;
    margin: 0 auto;
    padding: 0 20px;
  }

  @media (min-width: 768px) {
    h1 {
      font-size: 3rem;
    }
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 16px;
    position: relative;
    z-index: 1;
  }
</style>
